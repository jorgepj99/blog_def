{% extends 'base.html.twig' %}
{% block css %}
    .buttons{
        display:flex;
        justify-content:space-around;
    }
    .content{
        margin:2vh 2vw;
        display:grid;
        grid-template-rows:1fr 5fr 1fr;
        grid-gap:5vw;
    }
{% endblock %}
{% block body %}
    {{ parent() }}
    <div class="content">
        <div class="header">
            <p>Creado por {{ user.getUsername() }}</p>
            <p>Fecha: {{ createat }}</p>
        </div>
        <div class="row">
            {{ form_start(form) }}
            {{ form_widget(form) }}<br/>
            <input type="submit" class="btn btn-success" value="Create Post">
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}